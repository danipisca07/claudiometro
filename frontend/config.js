// Indirizzo base delle API claudiometro.
// Lascia stringa vuota ("") per usare lo stesso host che serve questa pagina.
// Per puntare a un altro host in LAN, imposta es: "http://192.168.1.50:4317"
window.CLAUDIOMETRO_API_BASE = "";

// Ogni quanti secondi la webapp aggiorna i gauge (polling). 0 = disabilita auto-refresh.
window.CLAUDIOMETRO_POLL_SECONDS = 30;
